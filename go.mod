// This module is effectively a protobuf-only module. This lets boundary
// and coder import this proto file without adding another dependency
// management system to the mix (e.g. a git submodule).
module github.com/coder/boundary-proto

go 1.24.10
